<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected, while bulk-selection mode is active (see RecordingsAdapter/LibraryFragment) —
         same accent color already used for the "currently active (playing)" left bar, at low
         alpha, so selection reads as a clear but not overpowering highlight. -->
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/library_selected_row_background" />
            <corners android:radius="8dp" />
            <stroke
                android:width="1dp"
                android:color="@color/library_active_accent" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="8dp" />
            <stroke
                android:width="1dp"
                android:color="@color/library_row_stroke" />
        </shape>
    </item>
</selector>
