<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected, while bulk-selection mode is active (see RecordingsAdapter/LibraryFragment):
         the accent container with an accent outline. The row's check box and its spoken
         "Selected" state carry the same meaning without relying on this color. -->
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimaryContainer" />
            <corners android:radius="@dimen/corner_card" />
            <stroke
                android:width="@dimen/stroke_hairline"
                android:color="?attr/colorPrimary" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="@dimen/corner_card" />
            <stroke
                android:width="@dimen/stroke_hairline"
                android:color="?attr/colorOutlineVariant" />
        </shape>
    </item>
</selector>
